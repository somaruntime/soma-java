package io.github.somaruntime.soma;

/** Stable public logical operation categories. */
public enum SomaOperation {
    CONFIGURE,
    RESERVE,
    ADD,
    FIND,
    GET,
    UPDATE,
    REMOVE,
    QUERY
}
